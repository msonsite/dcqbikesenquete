name: Deploy naar GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node.js setup
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Dependencies installeren
        run: npm ci

      - name: Next.js build (static export)
        run: npm run build:pages
        env:
          NEXT_PUBLIC_BASE_PATH: /dcqbikesenquete
          GITHUB_PAGES: "true"
          # De publishable key is openbaar (staat sowieso in de browser-code);
          # data blijft beschermd door Row Level Security. Secrets krijgen voorrang
          # indien ingesteld, anders wordt onderstaande fallback gebruikt.
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://vvyglzwbhoxinsifehvy.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'sb_publishable_hcKOdXUO6UcSmfLX77Nkxg_qu-R_-Mr' }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy naar GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
